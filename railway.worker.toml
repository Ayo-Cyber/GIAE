[build]
  builder        = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "celery -A giae_api.worker.celery_app worker --loglevel=info --pool=threads --concurrency=4"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 5
